---
import { getCollection, render } from 'astro:content';
import MainLayout from '../../layouts/MainLayout.astro';
import Breadcrumbs from '../../components/molecules/Breadcrumbs.astro';
import Badge from '../../components/atoms/Badge.astro';
import Button from '../../components/atoms/Button.astro';
import Icon from '../../components/atoms/Icon.astro';
import { getCategoryBySlug, getSubcategoryBySlug } from '../../data/categories';

export async function getStaticPaths() {
  const entries = await getCollection('workflows');
  return entries.map((entry) => ({
    params: { slug: entry.id },
    props: { entry },
  }));
}

const { entry } = Astro.props;
const { Content } = await render(entry);
const workflow = entry.data;
const rawContent = entry.body || '';

const category = getCategoryBySlug(workflow.categorySlug);
const subcategory = getSubcategoryBySlug(workflow.categorySlug, workflow.subcategorySlug);

const breadcrumbs = [
  { label: 'Home', href: '/' },
  { label: 'Workflows', href: '/workflows' },
  { label: category?.name || 'Category', href: `/categories/${workflow.categorySlug}` },
  { label: workflow.title },
];
---

<MainLayout
  title={`${workflow.title} — LunaPrompt`}
  currentPath="/workflows"
>
  <Breadcrumbs items={breadcrumbs} />

  <div class="workflow-detail">
    <div class="workflow-detail__main">
      <section class="workflow-detail__header">
        <h1 class="workflow-detail__title">{workflow.title}</h1>
        <div class="workflow-detail__badges">
          <Badge variant="accent" size="md">{workflow.difficulty}</Badge>
          <Badge variant="outline" size="md">{subcategory?.name}</Badge>
          {workflow.targetAgent && (
            <Badge variant="outline" size="md">{workflow.targetAgent}</Badge>
          )}
        </div>
      </section>

      <section class="workflow-detail__overview">
        <div class="workflow-detail__overview-card">
          <h2 class="workflow-detail__section-title">Overview</h2>
          <p class="workflow-detail__description">{workflow.description}</p>
        </div>
      </section>

      <section class="workflow-detail__content">
        <div class="workflow-detail__content-card">
          <div class="workflow-detail__content-header">
            <div class="workflow-detail__content-dots">
              <span class="dot dot--red"></span>
              <span class="dot dot--yellow"></span>
              <span class="dot dot--green"></span>
            </div>
            <span class="workflow-detail__content-filename">{entry.id}.md</span>
            <button class="workflow-detail__copy-btn" id="copy-workflow-btn" aria-label="Copy workflow content">
              <Icon name="copy" size={14} />
              <span class="workflow-detail__copy-text">Copy</span>
            </button>
            <button class="workflow-detail__toggle-btn" id="toggle-view-btn" aria-label="Toggle raw/rendered view">
              <Icon name="code" size={14} />
              <span class="workflow-detail__toggle-text">Raw</span>
            </button>
          </div>
          <div class="workflow-detail__content-body" id="workflow-content">
            <Content />
          </div>
          <pre class="workflow-detail__content-raw" id="workflow-content-raw" hidden><code>{rawContent}</code></pre>
        </div>
      </section>
    </div>

    <aside class="workflow-detail__sidebar">
      <div class="workflow-detail__preview">
        <div class="workflow-detail__preview-visual">
          <Icon name="git-branch" size={48} />
        </div>
      </div>

      <div class="workflow-detail__meta-card">
        <h3 class="workflow-detail__meta-title">Detalles</h3>
        <div class="workflow-detail__meta-row">
          <span class="workflow-detail__meta-label">Pasos</span>
          <span class="workflow-detail__meta-value">{workflow.steps}</span>
        </div>
        {workflow.estimatedTime && (
          <div class="workflow-detail__meta-row">
            <span class="workflow-detail__meta-label">Tiempo estimado</span>
            <span class="workflow-detail__meta-value">{workflow.estimatedTime}</span>
          </div>
        )}
        <div class="workflow-detail__meta-row">
          <span class="workflow-detail__meta-label">Dificultad</span>
          <Badge variant="accent" size="sm">{workflow.difficulty}</Badge>
        </div>
        {workflow.targetAgent && (
          <div class="workflow-detail__meta-row">
            <span class="workflow-detail__meta-label">Agente</span>
            <span class="workflow-detail__meta-value">{workflow.targetAgent}</span>
          </div>
        )}
        <div class="workflow-detail__meta-row">
          <span class="workflow-detail__meta-label">Categoría</span>
          <span class="workflow-detail__meta-value">{category?.name}</span>
        </div>
        <div class="workflow-detail__meta-row">
          <span class="workflow-detail__meta-label">Creado</span>
          <span class="workflow-detail__meta-value">{workflow.createdAt}</span>
        </div>
        <div class="workflow-detail__meta-tags">
          {workflow.tags.map((tag) => (
            <Badge variant="outline" size="sm">{tag}</Badge>
          ))}
        </div>
        <Button variant="secondary" fullWidth href="/workflows">
          <span>Ver todos los Workflows</span>
        </Button>
      </div>
    </aside>
  </div>
</MainLayout>

<style>
  .workflow-detail {
    display: grid;
    grid-template-columns: 1fr 288px;
    gap: var(--space-8);
    margin-top: var(--space-8);
  }

  .workflow-detail__main {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
    min-width: 0;
  }

  .workflow-detail__header {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .workflow-detail__title {
    font-size: var(--text-3xl);
    font-weight: var(--weight-bold);
    color: var(--color-text-primary);
  }

  .workflow-detail__badges {
    display: flex;
    gap: var(--space-2);
    flex-wrap: wrap;
  }

  .workflow-detail__overview-card {
    padding: var(--space-6);
    background: var(--color-bg-card);
    border: 1px solid var(--color-border-card);
    border-radius: var(--radius-lg);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .workflow-detail__section-title {
    font-size: var(--text-2xl);
    font-weight: var(--weight-semibold);
    color: var(--color-text-primary);
  }

  .workflow-detail__description {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
  }

  .workflow-detail__content-card {
    background: var(--color-code-bg);
    border: 1px solid var(--color-code-border);
    border-radius: var(--radius-lg);
    overflow: hidden;
  }

  .workflow-detail__content-header {
    display: flex;
    align-items: center;
    padding: var(--space-3) var(--space-4);
    border-bottom: 1px solid var(--color-code-border);
    background: var(--color-bg-secondary);
  }

  .workflow-detail__content-dots {
    display: flex;
    gap: 6px;
    margin-right: var(--space-4);
  }

  .dot {
    width: 8px;
    height: 8px;
    border-radius: var(--radius-full);
  }

  .dot--red { background: #ef4444; }
  .dot--yellow { background: #f59e0b; }
  .dot--green { background: #22c55e; }

  .workflow-detail__content-filename {
    flex: 1;
    text-align: center;
    color: var(--color-text-tertiary);
    font-size: var(--text-xs);
    font-family: var(--font-mono);
  }

  .workflow-detail__copy-btn {
    display: flex;
    align-items: center;
    gap: var(--space-1);
    padding: var(--space-1) var(--space-3);
    border-radius: var(--radius-sm);
    color: var(--color-text-tertiary);
    font-size: var(--text-xs);
    background: var(--color-bg-elevated);
    border: 1px solid var(--color-border-default);
    cursor: pointer;
    transition: all var(--transition-fast);
  }

  .workflow-detail__copy-btn:hover {
    color: var(--color-text-primary);
    border-color: var(--color-accent-primary);
  }

  .workflow-detail__toggle-btn {
    display: flex;
    align-items: center;
    gap: var(--space-1);
    padding: var(--space-1) var(--space-3);
    margin-left: var(--space-2);
    border-radius: var(--radius-sm);
    color: var(--color-text-tertiary);
    font-size: var(--text-xs);
    background: var(--color-bg-elevated);
    border: 1px solid var(--color-border-default);
    cursor: pointer;
    transition: all var(--transition-fast);
  }

  .workflow-detail__toggle-btn:hover {
    color: var(--color-text-primary);
    border-color: var(--color-accent-primary);
  }

  .workflow-detail__content-raw {
    padding: var(--space-6);
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
    font-family: var(--font-mono);
    white-space: pre-wrap;
    word-wrap: break-word;
    overflow-x: auto;
    margin: 0;
  }

  .workflow-detail__content-body {
    padding: var(--space-6);
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
  }

  .workflow-detail__content-body :global(h1),
  .workflow-detail__content-body :global(h2),
  .workflow-detail__content-body :global(h3) {
    color: var(--color-text-primary);
    margin-top: var(--space-4);
    margin-bottom: var(--space-2);
  }

  .workflow-detail__content-body :global(code) {
    padding: 2px 6px;
    background: var(--color-bg-elevated);
    border: 1px solid var(--color-border-default);
    border-radius: var(--radius-sm);
    font-family: var(--font-mono);
    font-size: var(--text-xs);
  }

  .workflow-detail__content-body :global(pre) {
    padding: var(--space-4);
    background: var(--color-bg-primary);
    border: 1px solid var(--color-border-default);
    border-radius: var(--radius-md);
    overflow-x: auto;
    margin: var(--space-4) 0;
  }

  .workflow-detail__content-body :global(pre code) {
    padding: 0;
    background: none;
    border: none;
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
  }

  .workflow-detail__content-body :global(ul),
  .workflow-detail__content-body :global(ol) {
    padding-left: var(--space-5);
    margin: var(--space-3) 0;
  }

  .workflow-detail__content-body :global(li) {
    margin-bottom: var(--space-2);
  }

  .workflow-detail__content-body :global(p) {
    margin-bottom: var(--space-3);
  }

  .workflow-detail__sidebar {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }

  .workflow-detail__preview {
    border-radius: var(--radius-lg);
    overflow: hidden;
    border: 1px solid var(--color-border-card);
  }

  .workflow-detail__preview-visual {
    display: flex;
    align-items: center;
    justify-content: center;
    height: 160px;
    background: linear-gradient(135deg, var(--color-bg-card) 0%, rgba(124, 58, 237, 0.08) 100%);
    color: var(--color-accent-secondary);
    opacity: 0.6;
  }

  .workflow-detail__meta-card {
    padding: var(--space-6);
    background: var(--color-bg-card);
    border: 1px solid var(--color-border-card);
    border-radius: var(--radius-lg);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .workflow-detail__meta-title {
    font-size: var(--text-base);
    font-weight: var(--weight-semibold);
    color: var(--color-text-primary);
  }

  .workflow-detail__meta-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .workflow-detail__meta-label {
    font-size: var(--text-sm);
    color: var(--color-text-tertiary);
  }

  .workflow-detail__meta-value {
    font-size: var(--text-sm);
    font-weight: var(--weight-medium);
    color: var(--color-text-secondary);
  }

  .workflow-detail__meta-tags {
    display: flex;
    gap: var(--space-1);
    flex-wrap: wrap;
    padding-top: var(--space-2);
    border-top: 1px solid var(--color-border-default);
  }

  @media (max-width: 1024px) {
    .workflow-detail {
      grid-template-columns: 1fr;
    }

    .workflow-detail__sidebar {
      order: -1;
    }

    .workflow-detail__preview {
      display: none;
    }
  }

  @media (max-width: 768px) {
    .workflow-detail__title {
      font-size: var(--text-2xl);
    }
  }
</style>

<script>
  const copyBtn = document.getElementById('copy-workflow-btn');
  const toggleBtn = document.getElementById('toggle-view-btn');
  const workflowContent = document.getElementById('workflow-content');
  const workflowRaw = document.getElementById('workflow-content-raw');

  if (copyBtn && workflowContent && workflowRaw) {
    copyBtn.addEventListener('click', async () => {
      const isRaw = workflowRaw && !workflowRaw.hidden;
      const text = isRaw
        ? (workflowRaw.textContent || '')
        : (workflowContent.innerText || workflowContent.textContent || '');

      try {
        await navigator.clipboard.writeText(text);
        const textEl = copyBtn.querySelector('.workflow-detail__copy-text');
        if (textEl) textEl.textContent = 'Copied!';
        setTimeout(() => { if (textEl) textEl.textContent = 'Copy'; }, 2000);
      } catch {
        const textarea = document.createElement('textarea');
        textarea.value = text;
        textarea.style.position = 'fixed';
        textarea.style.opacity = '0';
        document.body.appendChild(textarea);
        textarea.select();
        document.execCommand('copy');
        document.body.removeChild(textarea);
        const textEl = copyBtn.querySelector('.workflow-detail__copy-text');
        if (textEl) textEl.textContent = 'Copied!';
        setTimeout(() => { if (textEl) textEl.textContent = 'Copy'; }, 2000);
      }
    });
  }

  if (toggleBtn && workflowContent && workflowRaw) {
    toggleBtn.addEventListener('click', () => {
      const isCurrentlyRaw = !workflowRaw.hidden;
      const textEl = toggleBtn.querySelector('.workflow-detail__toggle-text');
      if (isCurrentlyRaw) {
        workflowRaw.hidden = true;
        workflowContent.hidden = false;
        if (textEl) textEl.textContent = 'Raw';
      } else {
        workflowRaw.hidden = false;
        workflowContent.hidden = true;
        if (textEl) textEl.textContent = 'Rendered';
      }
    });
  }
</script>
